resources:
  requests:
    cpu: "50m"
    memory: "64Mi"
  limits:
    cpu: "200m"
    memory: "128Mi"

persistence:
  size: 1Gi
  # standard = StorageClass di default minikube per test
  storageClass: standard
